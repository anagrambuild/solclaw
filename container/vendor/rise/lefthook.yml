min_version: 1.11.12

output:
  - failure

pre-commit:
  parallel: true
  commands:
    rustfmt:
      glob: "*.rs"
      run: rustfmt +nightly --edition 2021 {staged_files}
      tags: [lint, fmt, rust]
      stage_fixed: true
